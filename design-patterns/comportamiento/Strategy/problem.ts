function chooseTransport(mode: string): void {
  if (mode === 'car') {
    console.log(`mode selected ${mode}`);
  } else if (mode == 'bike') {
    console.log(`mode selected ${mode}`);
  } else if (mode == 'bike') {
    console.log(`mode selected ${mode}`);
  } else if (mode == 'bus') {
    console.log(`mode selected ${mode}`);
  } else if (mode == 'uber') {
    console.log(`mode selected ${mode}`);
  } else if (mode == 'didi') {
    console.log(`mode selected ${mode}`);
  } else if (mode == 'cabify') {
    console.log(`mode selected ${mode}`);
  } else if (mode == 'etc') {
    console.log(`mode selected ${mode}`);
  } else if (mode == 'etc') {
    console.log(`mode selected ${mode}`);
  } else if (mode == 'bike') {
    console.log(`mode selected ${mode}`);
  } else if (mode == 'bike') {
    console.log(`mode selected ${mode}`);
  } else if (mode == 'bike') {
    console.log(`mode selected ${mode}`);
  }
}